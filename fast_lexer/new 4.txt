while(state != FINAL_STATE)
		{

			state = 0;
			start_i = i;
			
			do { state = transition_table[state + input[i++]]; } while(state < FINAL_STATE);
			
			if(state < NON_CONTRAIGNANT_STATE) i--;
			
			const(int) token = (state-FINAL_STATE)/256;
			
			auto token_content = input[start_i .. i];
			
			/*writeln("(", token, ") ", token_content);*/
			 
		}