
model
class Object
{
	model toString: String
	{
		ret "Hey" + {50, 784, 111} + CLASS.name;
	}
}

procedure(int index): int
{
    ret index * 6;
}

template<SIZE>
wrap(int size): &*[SIZE]int;

template<DEST, SRC>
copy(ptr dest, ptr src): DEST.type;

template<SIZE>
alloc(int size): *[SIZE]int;

template<...AGRS>
script MultiArgsInOnce
print;

print(int arg) { let a = itoa(arg); write(1, a, len(a)); }


&*[50]int array = wrap(50);

array[0] = &HashSet<String>;